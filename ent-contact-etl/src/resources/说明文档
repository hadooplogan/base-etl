  									联系信息库
  	一，优先级指定
  	   优先级根据数据源了解的质量情况制定，以后会根据实际使用情况进行优先级修改，优先级顺序如下：
  	   1，2016年报
  	   2，2015年报（没有pripid，所以需要通过entname进行关联获得pripid）
  	   3，照面信息
 	   4，税务信息登记
 	   5，人员信息
       （数据源本身存在重复数据,取了三个职位，法人，董事长，总经理）

     二，关于对数据的清洗
         主要清洗了两个部分，tel（电话），email（邮箱）
         1，Tel：      （1）00000000诸如此类重复数字，对大于七位以上重复数字清洗
                      （2）12，1234，5677，对小于5位的数据进行清洗。
                      （3）特殊字符，单个字母，-，.,*.
                       (4)13******789,此类中间含有大量重复符号的电话进行清洗
                      （5）"" 此类连续的符号
         2，email：    （1）清洗了 如同此种000@qq.com的邮箱
           			  （2） 不符合邮箱规则的邮箱

         3，address： 长度大于6


    三，优先级规则
          （1） 在营业数据采用的是 按照时间节点进行排序，没有数据源优先级（数据更新日期最新） 字段  UDT(udt字段并未存入es，仅仅存在hdfs上为了对数据进行排序，es上的date是跑批时间)
           (2)  全量数据  对数据清洗后进行了merge，使用 pripid+sequence+数据来源标识 作为 es的绝度主键。


    四，生产上的配置文件
          （1）table.list全量数据配置文件
          （2）table_inc.list增量数据配置文件， 文件中的路径中将inc改成del后就是存储del的文件路径。


    五，两个索引之间的关系
        在营企业索引是在全量索引的hdfs文件基础上跑出的在营企业数据。


    六，索引更新

         历史hdfs数据存储，每次根据增量文件和历史文件中字段进行比对，符合规则且不为空的数据会更新，且更新数据日期。
         对于es更新是es有的数据与从、历史存在的数据分别进行 insert和update。

         在营企业存在数据删除，企业状态不为在营，则删除。 在增量数据比对时确人企业是否在营，将删除id存储在hdfs上，读取id进行es索引维护。